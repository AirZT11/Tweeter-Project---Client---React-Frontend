import React, { Component } from 'react';
import { connect } from 'react-redux';
import { withRouter } from 'react-router';

const AllUsers = props => {

  return (
    <div>
      
    </div>
  )
}

export default withRouter(connect(null, {})(AllUsers))